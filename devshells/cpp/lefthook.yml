# lefthook.yml

pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{cpp,h}"
      run: just format {staged_files} && git add {staged_files}

pre-push:
  parallel: false
  commands:
    test:
      run: just test
